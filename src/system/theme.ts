const coral = "#C95E64"
const golden = "#C49A70"
const lightTeal = "#A9CDC9"
const darkTeal = "#729C98"

const lightGray = "#E3E8ED"
const gray = "#D1D4DC"
const mediumGray = "#959BA7"
const blueGray = "#565A63"
const darkGray = "#242933"

const body = "objektiv-mk1, sans-serif" // 300 ou 400
const header = "objektiv-mk1, sans-serif" // 700
const accent = "attribute-mono, sans-serif" // 300 ou 400

export const themeFonts = {
  body,
  header,
  accent,
}

export const themeColors = {
  coral,
  golden,
  lightTeal,
  darkTeal,
  lightGray,
  gray,
  mediumGray,
  blueGray,
  darkGray,
}

// default : mobile
// iPhone 11 - 4 cols
const xsm = `424px`

// iPad - 8 cols
const sm = `768px`

// iPad Pro
const md = `1024px`

// iMac - 12 cols
const lg = `1280px`

// macbook pro
const xl = `1440px`

export const themeBreakpoints = {
  xsm,
  sm,
  md,
  lg,
  xl,
}
